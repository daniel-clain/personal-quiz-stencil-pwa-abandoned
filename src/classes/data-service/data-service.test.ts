describe('data service', () => {
    describe('observes connection to remote db and triggers script on connect', () => {
        
    });
    describe('on start, triggers local db service to run its setup', () => {
        
    });
    describe('delegates crud operations to local db service, and if connected to remote db service', () => {
        
    });
});